// Configuration options for the browser
export default {
    // Default search engine (google, duckduckgo, or bing)
    searchEngine: 'bing',
    
    // Animation settings
    animations: {
        enabled: true,
        tabTransitionDuration: 0.2,
        pageLoadDuration: 0.8
    },
    
    // Interface settings
    interface: {
        darkMode: false,
        accentColor: '#0A84FF'
    },

    // Default new tab URL
    defaultSearchUrl: 'https://www.bing.com/search'
};